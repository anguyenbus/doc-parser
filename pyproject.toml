[project]
name = "parser-service"
version = "0.1.0"
description = "Document parsing service: Docling + AWS Bedrock Claude Sonnet hybrid pipeline"
requires-python = ">=3.11"
dependencies = [
    # tested with 2.x.x — pin exact version after first successful run
    "docling>=2.0",
    "pypdfium2>=4.30",
    "Pillow>=10.0",
    "boto3>=1.34",
    "pydantic>=2.7",
    "pypdf>=6.14.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.4",
    "mypy>=1.10",
    "pre-commit>=3.7",
    # Only needed to create fixture files programmatically
    "python-docx>=1.1",
    "openpyxl>=3.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/parser_service"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.mypy]
strict = true
python_version = "3.11"
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "live: requires real Bedrock credentials (manual or nightly only)",
    "slow: full eval-harness run, weekly only",
]
